BARSAN R35.2.3 — Railway Final Checklist

[ ] فایل‌های ZIP مستقیم در ریشهٔ repository قرار گرفته‌اند.
[ ] Dockerfile، railway.json، requirements.lock.txt، pyproject.toml و main.py کنار هم هستند.
[ ] Root Directory سرویس خالی است (repository root)؛ مسیر پوشهٔ نسخهٔ قدیمی تنظیم نشده است.
[ ] متغیر RAILWAY_DOCKERFILE_PATH قدیمی حذف شده است.
[ ] Build Command و Start Command دستی در Dashboard خالی هستند.
[ ] Deployment Details نشان می‌دهد Dockerfile ریشه مصرف شده است.
[ ] Build log هیچ COPY مربوط به pyproject.toml نشان نمی‌دهد.
[ ] Volume با Mount Path=/data متصل است.
[ ] Replica برابر 1 است.
[ ] JWT_SECRET فقط در Railway Variables و حداقل 32 کاراکتر است.
[ ] PROVIDER_KEY_ENCRYPTION_SECRET فقط در Railway Variables و حداقل 32 کاراکتر است.
[ ] INITIAL_ADMIN_PASSWORD فقط در Railway Variables تعریف شده است.
[ ] برای provider انتخابی کلید API لازم تعریف شده است.
[ ] PORT دستی تعریف نشده و توسط Railway تزریق می‌شود.
[ ] Public Domain به سرویس web متصل شده است.
[ ] Start log شامل BARSAN_START mode=docker است.
[ ] /healthz پاسخ 200 و status=ok می‌دهد.
[ ] /readyz پاسخ 200 و status=ready می‌دهد.
[ ] Build Cache قدیمی پیش از نخستین Deploy این نسخه پاک شده است.

